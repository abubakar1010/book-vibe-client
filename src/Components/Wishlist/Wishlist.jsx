import { Button } from "@material-tailwind/react";

const Wishlist = () => {
    return (
        <>
            <div>
                <Button>Wishlist</Button>
            </div>
        </>
    );
};

export default Wishlist;